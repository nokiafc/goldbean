<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tianmi.goldbean">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_app"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".login.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.LoginActivity"/>
        <activity android:name=".login.RegisterActivity"/>
        <activity android:name=".login.ForgetPsdActivity"/>
        <activity android:name=".login.ChangePsdActivity"/>
        <activity android:name=".MainActivity"/>
        <activity android:name=".main.RoomActivity"/>
        <activity android:name=".my.RechargeActivity"/>
        <activity android:name=".my.RechargeSuccessActivity"/>
        <activity android:name=".my.CashActivity"/>

    </application>

</manifest>